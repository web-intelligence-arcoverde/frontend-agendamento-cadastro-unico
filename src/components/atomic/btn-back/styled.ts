import styled from 'styled-components';
import { COLORS } from 'src/constants';

export const Container = styled.div`

button {
  display: block;
  background-color:${COLORS.ForGreen};
  padding: 0;
  border-radius: 100%;
  border: none;
  position: fixed;
  bottom:2%;
  right: 5%;

}

img {
  width: 50px;
  
}
`;
